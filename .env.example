# required: your replicate api token, get one at https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# optional: per-ip rate limit when the RATE_LIMIT_KV binding is set
# leave unset to default to 20 generations per hour per ip
RATE_LIMIT_PER_HOUR=20
